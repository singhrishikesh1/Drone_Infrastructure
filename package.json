{
  "name": "drone-infrastructure-root",
  "version": "2.0.0",
  "private": true,
  "description": "Autonomous AI-Powered Drone Infrastructure Inspection & Telemetry Platform",
  "scripts": {
    "start": "concurrently \"npm --prefix backend start\" \"npm --prefix frontend dev\"",
    "dev": "concurrently \"npm --prefix backend start\" \"npm --prefix frontend dev\"",
    "build": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
